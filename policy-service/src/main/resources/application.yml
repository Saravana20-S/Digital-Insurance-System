spring:
  application:
    name: policy-service

  profiles:
    active: dev

server:
  port: 8082

# Eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

    register-with-eureka: true
    fetch-registry: true

  instance:
    prefer-ip-address: true

# Logging
logging:
  level:
    root: INFO
    com.digitalinsurance: DEBUG